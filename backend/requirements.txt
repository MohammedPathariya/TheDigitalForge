# Core libraries
flask
flask-cors
python-dotenv

# CrewAI framework
crewai

# Markdown conversion
markdown

# Testing
pytest

# OpenAI (if you're using it anywhere inside CrewAI tools or custom models)
openai

# Optional: Used by some CrewAI dependencies (safe to include)
requests
tiktoken
pydantic